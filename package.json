{
  "name": "mcp-digisac",
  "version": "0.1.0",
  "description": "MCP server for Digisac - connect AI assistants to Digisac REST API via MCP protocol",
  "type": "module",
  "bin": {
    "mcp-digisac": "dist/index.js"
  },
  "files": [
    "dist/",
    "README.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "test": "npm run build && node --test tests/unit/*.test.mjs tests/e2e/*.test.mjs",
    "lint:stdout": "if grep -rn 'console\\.log' src/; then echo 'console.log forbidden in src/' >&2; exit 1; fi"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "@types/node": "^20"
  }
}
